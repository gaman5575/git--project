pipeline {
    agent any

    stages {
        stage('Trigger other pipelines') {
            steps {
                trigger([
                    triggerRemoteJob('https://github.com/gaman5575/local11', 'main', 'Jenkinsfile')
                ])
            }
        }
    }
}
